escribi un nuevo spec en la carpeta de specs que sea para iterar las configuraciones de usuario, por ejemplo que recuerde cosas como la config del windows watcher

o que recuerde el ultimo perfil seleccionado, el lenguaje

y.. esto no es una config, o tal vez si, pero quisiera que si la config tiene gestos, yo aprete el boton y ya me lleve de una a la tab de "Mouse Gestures" en vez de "Simple action", igual algun tipo de memoria ya tiene porque lo recuerda, pero me parece que es solo un state de react